<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
     <style>
            /* General Body Styling */
            body {
                font-family: 'Arial', sans-serif;
                margin: 0;
                padding: 0;
                background-color: #f4f9ff;
                color: #333;
            }

            /* Main Container Styling */
            .dashboard-main {
                max-width: 800px;
                margin: 50px auto;
                padding: 20px;
                background-color: #ffffff;
                border-radius: 8px;
                box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
                text-align: center;
            }

            h1 {
                color: #004aad;
                margin-bottom: 20px;
            }

            p {
                color: #555;
                font-size: 18px;
                margin-bottom: 30px;
            }

            /* Button Styling */
            .button {
                display: inline-block;
                padding: 12px 30px;
                margin: 15px;
                text-decoration: none;
                font-size: 16px;
                font-weight: bold;
                color: #ffffff;
                background-color: #ffcb05;
                border-radius: 6px;
                transition: all 0.3s ease-in-out;
            }

            .button:hover {
                background-color: #ffd633;
                color: #004aad;
            }

            /* Responsive Design */
            @media (max-width: 600px) {
                .dashboard-main {
                    margin: 20px;
                    padding: 15px;
                }

                h1 {
                    font-size: 22px;
                }

                p {
                    font-size: 16px;
                }

                .button {
                    padding: 10px 20px;
                    font-size: 14px;
                }
            }
        </style>
</head>
<body>
    <h1>Admin Dashboard</h1>
    <p>Welcome, Admin!</p>

    <div>
        <!-- Buttons for admin actions -->
        <a href="/admin/users" class="button">View and Manage Users</a>
        <a href="/admin/auctions" class="button">View and Manage Auctions</a>
       <a href="/logout" class="button">Logout</a>
    </div>
</body>
</html>
=======
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
     <style>
            /* General Body Styling */
            body {
                font-family: 'Arial', sans-serif;
                margin: 0;
                padding: 0;
                background-color: #f4f9ff;
                color: #333;
            }

            /* Main Container Styling */
            .dashboard-main {
                max-width: 800px;
                margin: 50px auto;
                padding: 20px;
                background-color: #ffffff;
                border-radius: 8px;
                box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
                text-align: center;
            }

            h1 {
                color: #004aad;
                margin-bottom: 20px;
            }

            p {
                color: #555;
                font-size: 18px;
                margin-bottom: 30px;
            }

            /* Button Styling */
            .button {
                display: inline-block;
                padding: 12px 30px;
                margin: 15px;
                text-decoration: none;
                font-size: 16px;
                font-weight: bold;
                color: #ffffff;
                background-color: #ffcb05;
                border-radius: 6px;
                transition: all 0.3s ease-in-out;
            }

            .button:hover {
                background-color: #ffd633;
                color: #004aad;
            }

            /* Responsive Design */
            @media (max-width: 600px) {
                .dashboard-main {
                    margin: 20px;
                    padding: 15px;
                }

                h1 {
                    font-size: 22px;
                }

                p {
                    font-size: 16px;
                }

                .button {
                    padding: 10px 20px;
                    font-size: 14px;
                }
            }
        </style>
</head>
<body>
    <h1>Admin Dashboard</h1>
    <p>Welcome, Admin!</p>

    <div>
        <!-- Buttons for admin actions -->
        <a href="/admin/users" class="button">View and Manage Users</a>
        <a href="/admin/auctions" class="button">View and Manage Auctions</a>
       <a href="/logout" class="button">Logout</a>
    </div>
</body>
</html>